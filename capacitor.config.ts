import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'zebra-example',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
